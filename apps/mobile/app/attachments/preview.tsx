// Route wrapper for the full-screen attachment preview. Params:
//   url (required), filename, mime.
// Pushed with `router.push({ pathname: '/attachments/preview', params })`.

export { default } from '../../src/features/attachments/AttachmentPreviewScreen';
