// Lucide icon for a household-wide budget. Per-category budgets reuse
// `categoryIconFor` from `../categories/icons`.

import { Wallet, type LucideIcon } from 'lucide-react-native';

export const HouseholdBudgetIcon: LucideIcon = Wallet;
