| Severity | Check | File:Line | Finding | Suggested Fix |
| -------- | ----- | --------- | ------- | ------------- |
| BLOCKER | JWT secret hardcoded fallback | apps/api/src/auth/jwt.strategy.ts:21; apps/api/src/auth/auth.service.ts:187,227,242 | `secretOrKey`/`secret` falls back to literal `'dev-access-secret-change-me'` when `JWT_ACCESS_SECRET` is unset. In a misconfigured prod boot the API will silently sign and accept tokens with a publicly known secret — full auth bypass. | Read once via `ConfigService` with a Zod-validated `ConfigModule.forRoot({ validationSchema })` that fails boot if `JWT_ACCESS_SECRET` is missing. Remove every `?? 'dev-...'` fallback. |
| BLOCKER | No env validation at boot | apps/api/src/app.module.ts:31 | `ConfigModule.forRoot({ isGlobal: true, cache: true })` has no `validationSchema`. Missing/typo'd `JWT_ACCESS_SECRET`, `DATABASE_URL`, `S3_*`, `SMTP_*`, `SESSION_SALT` boot a half-broken process instead of failing fast. | Pass a Zod (or Joi) schema covering all required env vars; combine with the secret-fallback removal above. |
| BLOCKER | OTP endpoints not rate-limited beyond the global default | apps/api/src/auth/auth.controller.ts:13,19,25,44; apps/api/src/app.module.ts:42 | Throttler has only one global bucket `{ ttl: 60_000, limit: 120 }`. There is no `@Throttle` override on `/auth/otp/request`, `/auth/otp/verify`, `/auth/otp/verify-for-security`, `/auth/refresh`. The DB-side per-email throttle in `requestOtp` (3/min) does not stop attacker IPs from rotating emails to enumerate users or burn OTP codes. | Register named throttlers (`short`/`medium`/`long`) and apply tight `@Throttle({ short: { limit: 5, ttl: 60_000 } })` to OTP/refresh routes. Consider keying by IP+email. |
| BLOCKER | PII (email addresses) written to logs | apps/api/src/mail/mail.service.ts:31,38,46,54,60 | Every send logs the recipient email at `log` level (`OTP mailed to ${email}`, `Email-change OTP mailed to ${newEmail}`, etc.). Violates the "no PII in logs" rule and writes user identifiers to centralized log sinks. | Drop the address (or hash/mask it — `m***@d***`). Pino redact list in `app.module.ts:39` only covers headers, not body interpolation. |
| BLOCKER | No global `JwtAuthGuard` — auth is opt-in per controller | apps/api/src/app.module.ts:64-67; controllers across `apps/api/src/**/*.controller.ts` | Spec demands "every endpoint protected by guard OR explicitly `@Public()`". Today auth is enforced via per-controller `@UseGuards(JwtAuthGuard, ...)`. Any new controller that forgets the decorator (e.g. `meta.controller.ts`, `weekly-summaries.controller.ts` both currently include it, but nothing structurally enforces it) ships unauthenticated by default. There is no `@Public()` decorator and no `APP_GUARD` JwtAuthGuard. | Register `JwtAuthGuard` as `APP_GUARD` (with a `Reflector`-driven `IS_PUBLIC_KEY`/`@Public()` escape hatch for `/auth/otp/*` and `/health`). Remove redundant per-controller `@UseGuards(JwtAuthGuard, ...)`. |
| BLOCKER | No production-mode stack-trace stripping in exception filter | apps/api/src/common/http-exception.filter.ts:16-46 | The filter never branches on `NODE_ENV`. For non-`HttpException` errors it logs the stack server-side (good) but the response body is the generic `{ code: INTERNAL_ERROR, message: 'Something went wrong.' }` — that is fine. However any thrown `Error` that *is* an `HttpException` subclass with a populated `message` will leak that message verbatim to clients. There is no allow/deny list, no env-aware redaction, no correlation id returned. | Make the filter env-aware: in prod return only `{code, message: <generic-for-status>}` for 5xx, attach `requestId` for client triage. Always log the full exception server-side with the correlation id. |
| MAJOR | `console.log` in bootstrap | apps/api/src/main.ts:25 | Bootstrap prints with `console.log` instead of the configured Pino `Logger`. Bypasses redaction, formatting, and log-level control. | Use `new Logger('Bootstrap').log(...)` or the pino logger pulled from `app.get(Logger)`. |
| MAJOR | `ZodValidationPipe` not wired via `APP_PIPE` | apps/api/src/main.ts:21; apps/api/src/app.module.ts | Registered via `app.useGlobalPipes(new ZodValidationPipe())` instead of `{ provide: APP_PIPE, useClass: ZodValidationPipe }`. As a result the pipe is not DI-aware, and `nestjs-zod`'s exception class is not bound to the global filter — every controller currently bypasses the pipe by calling `Schema.parse(q)` manually anyway (see widespread `Query() q: Record<string, string>` pattern below). | Wire via APP_PIPE and let `@Query()`/`@Body()` parameters be typed Zod DTOs; remove the `Record<string, string>` + manual `.parse(q)` shim. |
| MAJOR | Query/path params validated manually inside controllers, not by the pipe | apps/api/src/loans/loans.controller.ts:35-38; apps/api/src/budgets/budgets.controller.ts:36-38; apps/api/src/transactions/transactions.controller.ts:33-36; apps/api/src/accounts/accounts.controller.ts:33-36; apps/api/src/stats/stats.controller.ts:24-57; apps/api/src/notifications/notifications.controller.ts:38-40; apps/api/src/attachments/attachments.controller.ts:32-34,40-42 (≈18 endpoints) | Pattern `@Query() q: Record<string, string>` followed by `Schema.parse(q)` makes the global ZodValidationPipe a no-op for query strings. Validation errors thrown from `parse` arrive as raw `ZodError` instances rather than the `ZodValidationException` the filter knows how to map, so 400s come out as 500 with a stack trace logged. | Convert query schemas to `createZodDto`-backed DTOs and accept them as `@Query() q: ListLoansQueryDto`. Path/query coercion stays inside the schema (`z.coerce.number()`). |
| MAJOR | `@Body() body: unknown` + manual parse in controllers | apps/api/src/notifications/notifications.controller.ts:47-51; apps/api/src/attachments/attachments.controller.ts:40-43 | Same anti-pattern for body. Bypasses the global pipe and the global filter's Zod handling. | Use `createZodDto` DTOs. |
| MAJOR | Global API prefix is `api`, not `api/v1` | apps/api/src/main.ts:19-20 | `setGlobalPrefix('api')` + URI versioning with `defaultVersion: '1'` produces `/api/v1` paths only because every `@Controller({ version: '1' })` opts in. The CLAUDE.md/spec rule is "all routes mounted under `/api/v1` global prefix". Mixing prefix + per-controller version means a controller that omits `version: '1'` (e.g. `health.controller.ts`, `meta.controller.ts`) would mount at `/api/...` un-versioned. | Either set `setGlobalPrefix('api/v1')` and drop per-controller `version`, OR make versioning mandatory by enforcing `defaultVersion` on controllers without an explicit version. |
| MAJOR | No graceful shutdown hooks | apps/api/src/main.ts:10-26 | `app.enableShutdownHooks()` is not called. Prisma `onModuleDestroy` and the `ScheduleModule` cron handlers will not fire on SIGTERM. | Add `app.enableShutdownHooks()` before `app.listen()`. |
| MAJOR | No body-size limit set | apps/api/src/main.ts:10-26 | Express's default 100kb is fine for most routes but the spec demands explicit configuration. With attachments handled via S3 presigned URLs (good), forgetting to cap JSON could still be abused on `bulk-create` style endpoints. | `app.use(express.json({ limit: '256kb' }))` (or via `NestFactory.create` `bodyParser` + custom raw setup). |
| MAJOR | No correlation-id middleware | apps/api/src/common/context.middleware.ts; apps/api/src/app.module.ts | `nestjs-pino` autogenerates a request id internally but it is not propagated as `x-correlation-id` (no header read, no header echo). Cross-service tracing and client error reports cannot be tied back to logs. | Add a middleware that reads/generates `x-correlation-id`, sets it on the response, and stuffs it into the ALS store alongside `auth`/`householdId`. Include it in pino's `customProps`. |
| MAJOR | Throttler is in-memory only | apps/api/src/app.module.ts:42 | `ThrottlerModule.forRoot([{ ttl: 60_000, limit: 120 }])` uses default in-memory storage. Multi-pod prod will let attackers burn N×limit per minute. | Use `@nestjs/throttler` Redis storage. |
| MAJOR | Cross-module barrel-style import (`bills` re-exports `recordEvent` consumed by `loans`) | apps/api/src/loans/loans.service.ts:17 (`import { recordEvent } from '../bills/bills.service'`); also accounts/transactions/budgets/categories/me each define their own local `recordEvent` (apps/api/src/accounts/accounts.service.ts:526; apps/api/src/transactions/transactions.service.ts:705; apps/api/src/budgets/budgets.service.ts:1109; apps/api/src/me/me.service.ts:161; apps/api/src/categories/categories.service.ts:386; apps/api/src/transfers/transfers.service.ts:379) | A cross-cutting "append to events table" helper has been duplicated 7×, and the loans module reaches across to bills' copy — a clear module-boundary violation and DRY break. Risk: schemas drift (one helper escapes payload, others don't), and loans' tests/refactors now couple to bills. | Promote `recordEvent` to a dedicated `events/` module (or `common/events.ts`) exposing an `EventsService`. Inject it everywhere and delete the local copies. |
| MAJOR | `me.controller.ts` directly uses `PrismaService` for domain mutations | apps/api/src/me/me.controller.ts:50,62-64,228-251,254-257 | Push-token list/create/delete logic lives inside the controller (`this.prisma.notificationToken.findUnique/update/create/deleteMany`). Controllers should be thin shells; service-layer encapsulation is bypassed. | Move push-token logic into `MeService` (or a dedicated `PushTokensService`). |
| MAJOR | Files exceed the 500-LoC soft limit | apps/api/src/budgets/budgets.service.ts (1152); apps/api/src/bills/bills.service.ts (1069); apps/api/src/loans/loans.service.ts (984); apps/api/src/stats/stats.service.ts (879); apps/api/src/transactions/transactions.service.ts (747); apps/api/src/accounts/accounts.service.ts (561) | Six service files are ≥560 lines, two over 1k. Indicates god-services that mix CRUD, scheduling logic, DTO mapping, and SQL helpers. Reading and reviewing changes is painful and complexity per function trends high. | Split per concern: e.g. `bills.service.ts` → `BillsCrudService`, `BillPaymentsService`, `BillCycleService`; move DTO mappers to `bills.mapper.ts`; move raw SQL helpers to `bills.repo.ts`. |
| MAJOR | `LastSeenMiddleware` is registered as middleware AND as a provider, with `forRoutes('*')` running on unauthenticated routes | apps/api/src/app.module.ts:26-27,66,71-72 | The middleware short-circuits when `req.user` is missing — but as middleware it runs *before* the JwtAuthGuard, so `req.user` is always undefined and the entire body is dead code. Listing it both as a provider and via `consumer.apply(...)` is also redundant. | Convert to an interceptor (or run inside the JwtAuthGuard's `canActivate` after `super.canActivate`). Drop the duplicate provider entry. |
| MAJOR | `enableCors({ origin, credentials: true })` with comma-split env list | apps/api/src/main.ts:15,18 | An empty `API_CORS_ORIGINS` env var would set origin to `['']`, an unset value defaults to localhost only — fine — but a misconfiguration that includes `*` would be silently accepted by Express. There is no validation. | Validate the parsed list (no `*`, all are absolute origins) at config load. Fail boot on misconfig. |
| MAJOR | `assertRole` reads role via `getContext()?.householdRole` — pure-string compare, no enum | apps/api/src/loans/loans.service.ts:811-814; apps/api/src/common/household-header.guard.ts:54-57 | `roleAtLeast(role: string, ...)` accepts any string and silently returns `false` for unknowns. A typo in `householdRole` writes (or a future role rename) would lock everyone out *or* admit everyone depending on call site. | Type `householdRole` as `HouseholdRole | undefined` in `RequestContext`; export a Prisma-derived enum and use it everywhere instead of `string`. |
| MINOR | No `.env.example` under `apps/api/` | apps/api/ | Only a top-level `.env.example` exists. New backend contributors have no canonical list of API-required vars (`JWT_ACCESS_SECRET`, `DATABASE_URL`, `S3_*`, `SMTP_*`, `SESSION_SALT`, `EXPO_ACCESS_TOKEN`, `API_LOG_LEVEL`, `API_CORS_ORIGINS`, `API_PORT`). | Add `apps/api/.env.example` enumerating every var the API reads, with safe placeholders. |
| MINOR | Notifications controller mixes household-scoped and unscoped routes in one controller, with ad-hoc per-method `@UseGuards(HouseholdHeaderGuard)` | apps/api/src/notifications/notifications.controller.ts:25-77 | The class-level guard list omits `HouseholdHeaderGuard`, and 5 methods individually re-add it. Easy to forget on a new method. | Split into `NotificationsController` (household-scoped, class-level guard) and `NotificationsUserController` (`/notifications/unread-count`, user-scoped). |
| MINOR | `void (null as unknown as AnyTx);` keep-alive at end of file | apps/api/src/loans/loans.service.ts:984 | A `void` no-op exists solely to silence "unused import" for `AnyTx`. The type is unused — drop it. | Remove the import + the keep-alive line. |
| MINOR | `unknown` accepted on controller bodies despite "no any/unknown in domain code" rule | apps/api/src/notifications/notifications.controller.ts:47; apps/api/src/attachments/attachments.controller.ts:40 | `@Body() body: unknown` is a deliberate workaround for the absent global pipe (see MAJOR above). Once DTOs are wired, drop these. | Replace with `createZodDto`-backed DTOs. |
| SUGGESTION | Auth strategy hardcodes the dev fallback even when `ConfigService` returns `undefined` | apps/api/src/auth/jwt.strategy.ts:21 | Once env validation lands the `?? 'dev-access-secret-change-me'` becomes dead code; remove it for clarity even if env validation now guarantees presence. | Drop the fallback, type as `config.getOrThrow<string>('JWT_ACCESS_SECRET')`. |
| SUGGESTION | `JwtModule.register({})` provides no defaults | apps/api/src/auth/auth.module.ts:10 | All `signOptions`/`secret` are passed inline at every `jwt.sign`/`jwt.verify` call, repeating the same 4-line literal. | Use `JwtModule.registerAsync({ inject: [ConfigService], useFactory: cfg => ({ secret: cfg.getOrThrow('JWT_ACCESS_SECRET') }) })` and let `AuthService` call `this.jwt.sign(payload, { expiresIn })`. |
| SUGGESTION | `ScheduleModule.forRoot()` runs on every pod | apps/api/src/app.module.ts:43 | Multi-pod prod will run weekly summaries / bill scheduler / budget scheduler N times, leading to duplicate notifications. | Gate scheduler modules behind a `SCHEDULER_ENABLED` env flag, OR add a Postgres advisory-lock guard inside each `@Cron` handler. |
