// The canonical `formatMoney` lives in `accounts/currency.ts` and is
// re-exported from the package root. We keep this file as a placeholder so
// the existing `./utils` sub-export stays wired (other domains will add
// utilities here later).
export {};
